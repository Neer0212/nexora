import { Users, Crown, Repeat2, UserRound } from "lucide-react"

function money(value:number,code:string){return new Intl.NumberFormat("en-IN",{style:"currency",currency:code||"INR",maximumFractionDigits:0}).format(value)}
export default function CustomerOverview({businessName,currencyCode,customers}:{businessName:string;currencyCode:string;customers:Array<{id:string;name:string;orders:number;revenue:number;units:number;averageOrderValue:number;share:number}>}){const revenue=customers.reduce((s,c)=>s+c.revenue,0);const repeat=customers.filter(c=>c.orders>1).length;const top=customers[0];const concentration=top&&revenue?top.revenue/revenue*100:0;return <div className="min-h-full bg-[#F1F0F8] px-5 py-8 lg:px-10 lg:py-10"><div className="mx-auto max-w-[1250px]"><header className="mb-8"><p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-[#433D8B]">Customer intelligence</p><h1 className="mt-2 text-4xl font-semibold tracking-[-0.045em] text-[#17153B]">Understand who is driving the business.</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-[#68647A]">Customer-level patterns for {businessName}, calculated from connected order data.</p></header><div className="grid gap-4 md:grid-cols-3"><Metric icon={Users} label="Customers" value={customers.length.toLocaleString("en-IN")} detail="Unique connected customer IDs"/><Metric icon={Repeat2} label="Repeat customers" value={repeat.toLocaleString("en-IN")} detail="More than one active order"/><Metric icon={Crown} label="Top customer share" value={`${concentration.toFixed(1)}%`} detail={top?top.name:"No customer identified"}/></div><div className="mt-5 rounded-3xl border border-[#E7E4EF] bg-white p-6"><div className="flex items-center justify-between gap-4"><div><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-[#9A94A8]">Customer leaderboard</p><h2 className="mt-2 text-xl font-semibold text-[#17153B]">Revenue concentration</h2></div><UserRound className="h-5 w-5 text-[#433D8B]"/></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[700px]"><thead><tr className="border-b border-[#E7E4EF] text-left text-[9px] uppercase tracking-[0.14em] text-[#9A94A8]"><th className="px-3 py-3">Customer</th><th className="px-3 py-3 text-right">Orders</th><th className="px-3 py-3 text-right">Revenue</th><th className="px-3 py-3 text-right">AOV</th><th className="px-3 py-3 text-right">Share</th></tr></thead><tbody className="divide-y divide-[#E7E4EF]">{customers.slice(0,15).map((c)=><tr key={c.id} className="text-sm"><td className="px-3 py-4 font-medium text-[#17153B]">{c.name}</td><td className="px-3 py-4 text-right text-[#68647A]">{c.orders}</td><td className="px-3 py-4 text-right font-medium text-[#17153B]">{money(c.revenue,currencyCode)}</td><td className="px-3 py-4 text-right text-[#68647A]">{money(c.averageOrderValue,currencyCode)}</td><td className="px-3 py-4 text-right text-[#433D8B]">{c.share.toFixed(1)}%</td></tr>)}</tbody></table>{customers.length===0&&<div className="p-10 text-center text-sm text-[#68647A]">No customer IDs were detected in the connected order data.</div>}</div></div></div></div>}
function Metric({icon:Icon,label,value,detail}:{icon:React.ElementType;label:string;value:string;detail:string}){return <div className="rounded-2xl border border-[#E7E4EF] bg-white p-6"><Icon className="h-4 w-4 text-[#433D8B]"/><p className="mt-5 text-[10px] uppercase tracking-[0.15em] text-[#9A94A8]">{label}</p><p className="mt-2 text-2xl font-semibold text-[#17153B]">{value}</p><p className="mt-1 text-xs text-[#9A94A8]">{detail}</p></div>}
